/**
 * Placeholder shell. The real four-tab interface arrives in Batch 7; this exists
 * so the scaffold compiles and `tauri dev` opens a window we can look at.
 */
export default function App() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-3 bg-neutral-950 text-neutral-100">
      <h1 className="text-2xl font-semibold tracking-tight">Dev Control Center</h1>
      <p className="text-sm text-neutral-400">
        Scaffold is up. Domain layers land in Batch 2 through Batch 6.
      </p>
    </main>
  );
}
